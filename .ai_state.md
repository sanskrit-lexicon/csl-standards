# Project Objective: CSL Standards — TEI/OntoLex interoperability workbench for CDSL (MW-PWG-PWK)

A research instrument that maps CDSL dictionary records into TEI (archival) and
OntoLex/FrAC (semantic) profiles, tracks every loss as an evidence-bound report,
and publishes the analysis. Pipeline: `npm run build-pilot` (sample → neutral
model → kośa parse → loss reports → exporters → validators → analyze).

## ➡️ Next Steps (Queue)

- **(done) RNG validation runs** — portable toolchain (`npm run
  setup-external-tools` → `npm run validate-external`, docs/EXTERNAL_VALIDATION.md)
  compiles both ODDs and validates all 250 archival + 256 Lex-0 files with jing; it
  surfaced and we fixed 3 TEI-conformance bugs. Remaining: `pip install pyshacl`
  for the RDF/SHACL layer (still `skipped` without it).
- **(done)** OntoLex-Lexicog container: each dictionary is a
  `lexicog:LexicographicResource` → `lexicog:entry` → `lexicog:Entry` → describes
  lemma. SHACL split into Resource + Entry shapes; pySHACL validates all 250
  (found via `python -m pyshacl`). RDF/SHACL layer now lit up.
- (Open-ended) Demo narrative; more dictionaries beyond MW/PWG/PWK; execute the
  Lex-0 ODD Schematron with an external SVRL engine.
- Resize lever: `node scripts/sample-hard-cases.mjs --max N` (default 250).
  Validators and scope labels are count-agnostic, so resizing needs no code edits.

## 🚧 Current Work-In-Progress (WIP)

- Lex-0 Schematron SVRL on branch `feat/schematron-svrl` (PR pending): real SVRL
  engine (Saxon + ISO Schematron skeleton) runs the Lex-0 ODD's Schematron over all
  256 Lex-0 files in the external harness; 0 failed asserts. setup-external-tools
  fetches the skeleton + compiles the SVRL transform; harness env hooks
  CSL_STANDARDS_LEX0_SVRL + CSL_STANDARDS_SAXON_JAR. **Closes the last
  validation-hardening item** — RNG + Schematron + SHACL all run by real engines.
  Pending merge. Open items left: demo narrative; more dictionaries.

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)

- **Reproducibility**: generators honor `SOURCE_DATE_EPOCH` and otherwise omit
  `generatedAt` (scripts/lib/provenance.mjs), so `build-pilot` is byte-stable —
  no more timestamp churn.
- **Shared libs**: `scripts/lib/citations.mjs` (the `<ls>` parser, used by
  build-neutral + both exporters) and `scripts/lib/mw-senses.mjs` (MW sense
  segmenter). The neutral model is the canonical citation layer; archival TEI and
  OntoLex still re-extract independently (intentional — they keep a higher cap).
- **Committing churn pitfall**: `git checkout -- .` to clean EOL churn will revert
  *uncommitted* edits. Always commit real work *before* any cleanup; stage real
  files explicitly and skip EOL-only artifact churn.
- **Sampler invariants** (learned at 250): require a counterpart in all three
  dictionaries (drop MW-only); detect `hasHedge` on the *stored/compacted* raw so
  the phenomenon matches the truncated artifacts.
- **Central finding holds at 250**: TEI never lossy for Western cases, OntoLex
  never clean, the neutral lineage lane most lossy. Regenerate the analysis with
  `npm run analyze-loss`; full prose in docs/LOSS_ANALYSIS.md.
- `.claude/` (harness state) and `.github/scripts/__pycache__/` are gitignored /
  untracked — not part of the project.
- **Loss-report coverage closed**: `build-loss-reports.mjs` emits an
  evidence-class sub-typing family (`named-kosha-citation`, `citation-coordinate`,
  `editorial-reference`; all `target: ontolex`, `model-vocabulary-gap`) + a
  `data-quality`/`source-anomaly` report for `[sic]`. `analyze-loss` now reports
  `schemaPhenomenaNotEmitted: []` + `roadmapCausesNotEmitted: []`; only the
  `failure` *status* stays (legitimately) unused. Detectors are curated heuristics
  (kośa sigla, editorial abbrevs, coordinate regex, literal `[sic]`) → counts are a
  lower bound. `data-quality` added to the schema enum.
- **#29 left `ontolex-review.json` stale**: the container-refinement PR regenerated
  the individual `ontolex/*.json` graphs (83 nodes each) but not the review summary
  (still at 68-node, pre-#29 counts); this slice's `build-pilot` resynced it. When
  a PR changes an exporter, regenerate *all* derived review artifacts, not just the
  primary outputs.

## ✅ Completed (Recent only)

This session (PRs #7–#16, #19, all merged; plus a direct `main` chore commit):

- **#7** SKD kośa parser → 6 records + SLP1→IAST transliteration; work-vs-person
  `<title>`/`<author>` split.
- **#8** Month-3 loss-report analysis: `scripts/analyze-loss-reports.mjs` +
  `data/pilot/loss-analysis.json` + docs/LOSS_ANALYSIS.md.
- **#9** PWG/PWK source-collapse loss-report family (lineage evidence loss); new
  `editorial-compression` cause; `neutral` target.
- **#10** Materialize named-source citations in the neutral model (PWG/MW/PWK
  `<ls>`); Lex-0 carries them with `#dict-…` provenance.
- **#11** Reproducible `generatedAt` (SOURCE_DATE_EPOCH; provenance.mjs).
- **#12** Indigenous kośa sense/citation-fusion loss reports; `skd` source +
  `sanskrit-convention` cause.
- **#13** Public bilingual Loss Analysis page on the Observable site.
- **#14** DRY: shared `<ls>` parser (scripts/lib/citations.mjs).
- **#15** MW sense segmenter (scripts/lib/mw-senses.mjs): Lex-0 sense coverage
  11 → 44 of 50.
- **#16** Sense-level citation linkage: MW `<ls>` attached to the sense it attests.
- **#19** Extend the pilot 50 → 250 cases (review slice stays 15). Corpus: 250
  TEI/OntoLex/RDF + 256 Lex-0; 959 loss reports; 2501 named-source citations;
  224/250 entries with senses; 117 entries / 244 senses sense-linked. Validators
  + scope labels made count-agnostic.
- **chore** (`24ec30f`, direct to main): ignore `__pycache__`.
- **#20** TEI Lex-0 ODD (data/schema/tei-lex0-profile.odd.xml) + RNG validation
  wired into validate-external-profiles (gated on Java toolchain; skipped locally).
- **#21** PWG/PWK sense modeling (scripts/lib/pw-senses.mjs): German senses +
  sense-linked citations on records.{pwg,pwk}.senses in the neutral model.
- **#22** OntoLex-Lexicog multi-resource: lexicog:Entry per source dictionary,
  multilingual senses, sense-level frac:Attestations; OntoLex export now
  model-driven (no raw re-extraction).
- **#23** Paper figures generator (scripts/build-figures.mjs): reproducible SVG
  Figures 1/2/5 from loss-analysis.json -> data/pilot/figures/.
- **#24** Figures 3 & 4 (root modeling, compound split) as SVG concept diagrams
  grounded in real pilot exemplars (√ac; annavid). All 5 figures now generated.
- **#25** Extension proposal (docs/EXTENSION_PROPOSAL.md): Month-3 milestone
  deliverable — each loss-cause → a concrete TEI/OntoLex construct prototyped in
  the csl: namespace, with standardize-vs-local disposition.
- **#26** Code-review fixes: Figure 5 dynamic height (clip), OntoLex lemma
  lexicog:entry domain violation removed, sampler compactRaw de-dup.
- **#27** Cut **v0.2.0** (2026-06-15): consolidated [Unreleased] → dated release
  (Month-3 milestone), bumped package.json 0.1.0→0.2.0, relabelled old [1.0.0]→
  [0.1.0]. No git tag pushed yet (awaiting confirmation).
- **#28** Portable external-validation toolchain (`setup-external-tools` /
  `validate-external`, docs/EXTERNAL_VALIDATION.md); fixed 3 real TEI-conformance
  bugs the RNG validation exposed (duplicate xml:id, mixed sourceDesc content,
  misplaced @target).
- **#29** OntoLex-Lexicog container refinement (LexicographicResource → entry →
  Entry) + lit up SHACL (pySHACL via `python -m pyshacl`).
- **#30 emit-remaining**: evidence-class sub-typing family + `data-quality`
  anomaly close every loss-report coverage gap; corpus 959 → 1277; docs renumbered
  (model-vocabulary-gap now the leading cause, 49%).
- **#31 evidence-class-extension**: the §1 proposal made real —
  `csl:evidenceClass` (textual/hedge/kosha/editorial) + parsed
  `csl:citedWork`/`csl:citedRange` on every `frac:Attestation`, shared detector in
  scripts/lib/evidence.mjs, SHACL `sh:in` + JS-validated + pySHACL-conformant.
  `analyze-loss` `extensionCoverage`: 569/569 OntoLex model-vocab-gap losses map to
  an implemented construct (each report's `mappedAs`). Honest framing: the gap vs
  vanilla standards is still recorded; the remedy now exists as a validated
  artefact. Detector is title-case heuristic (all-caps kośa sigla like VAIJ. read
  as textual) → counts are a lower bound; intentionally not re-tuned to avoid
  re-churning #30's numbers.
- **#32 Cut v0.3.0** (2026-06-16, tag `v0.3.0` pushed): consolidated [Unreleased]
  (#28–#31) → dated release; bumped package.json 0.2.0→0.3.0. Theme: validation
  hardening + interoperability-modeling depth. Repo convention is annotated git
  tags only (no GitHub Releases); remote has `v0.2.0`, `v0.3.0`.
- **#33** journal hygiene (cleared stale WIP after v0.3.0).
- **#34 tei-evidence-class**: TEI side of the evidence-class extension, symmetric
  with #31 — `@subtype` (textual/hedge/kosha/editorial) + `<citedRange>` on every
  citation `<bibl>` in the archival + Lex-0 exports, shared scripts/lib/evidence.mjs,
  validated against the compiled TEI RELAX NG (jing). No ODD change (both come from
  the `core` module). Full external harness: 758 checks, 0 failed. First
  post-0.3.0 [Unreleased] entry.
- **#35** journal hygiene (cleared stale WIP after #34).
- **#36 source-collapse-lineage**: `csl:LineageRelation` makes the PWG→PWK→MW
  collapse explicit/queryable (abridgement + recomposition, with retained/dropped
  counts), SHACL-validated; `lineageCoverage` 369/369. A *modeling* construct for
  an upstream editorial loss (extensionNeeded:false), not a target extension —
  closes EXTENSION_PROPOSAL §4a, the largest loss family.
- **#37** journal hygiene (cleared stale WIP after #36).
- **#38 Cut v0.4.0** (2026-06-16, tag `v0.4.0` pushed): consolidated [Unreleased]
  (#34, #36) → dated release; bumped package.json 0.3.0→0.4.0. Theme: the extension
  proposal made real (§1/§2/§3/§4a implemented + schema-validated; extensionCoverage
  569/569, lineageCoverage 369/369). Remote tags: v0.2.0, v0.3.0, v0.4.0.
- **#39** journal hygiene (cleared WIP to the v0.4.0 tag).
- **#40 continuation-recovery-status**: §4 implemented —
  `csl:recoveryStatus` (recovered/conjectured/unresolved) on `csl:ContinuationRelation`
  + TEI `<xr>` `@subtype`, SHACL `csl:ContinuationRelationShape`, pySHACL + jing
  validated. All 40 continuation cases are `conjectured` (eCode pointer known, parent
  not asserted).
- **#41 kosa-sense-boundary**: §5 implemented — typed
  `<bibl type="kosa-authority">` + `<note type="entry-convention">kosa-iti-unit</note>`
  + a per-iti-unit `<note type="model-loss">` (default when the fixture gives none);
  ODD `csl-lex0-kosa-sense-boundary` Schematron tightened; validate-tei-lex0 enforces
  it in build-pilot; all 6 kośa files jing-validated. **EXTENSION_PROPOSAL §1–§5 + §4a
  are all implemented now.** Note: not every authority sense had an explicit `loss`,
  so the export emits a default fusion note — the construct is uniform per iti-unit.
- **#42** journal hygiene (cleared stale WIP after #41).
- **#43 Cut v0.5.0** (2026-06-16, tag `v0.5.0` pushed): consolidated [Unreleased]
  (#40, #41) → dated release; bumped package.json 0.4.0→0.5.0. Theme: the extension
  proposal complete — every construct (§1–§5, §4a) implemented + validated
  in-pipeline (pySHACL + jing). Remote tags: v0.2.0, v0.3.0, v0.4.0, v0.5.0.
- **#44** journal hygiene (cleared WIP to the v0.5.0 tag).
- **#45 paper-prose**: docs/PAPER.md — full draft of the article, elaborating
  PAPER_OUTLINE with a Results section on the implemented + validated extension
  layer (asymmetry, evidence-centred diagnosis, lineage, kośa, coverage metrics,
  external-harness validation). Outline retained as the planning doc; README +
  EXTENSION_PROPOSAL README description updated (proposed → implemented).
- **#46** journal hygiene (cleared stale WIP after #45).
- **#47 Cut v0.6.0** (2026-06-16, tag `v0.6.0` pushed): consolidated [Unreleased]
  (paper draft) → dated release; bumped package.json 0.5.0→0.6.0. Theme: the
  write-up (docs/PAPER.md), documentation only. Remote tags: v0.2.0–v0.6.0.
- **#48** journal hygiene (cleared WIP to the v0.6.0 tag).
- **schematron-svrl** (this slice): real SVRL engine for the Lex-0 ODD Schematron
  (Saxon + ISO Schematron skeleton); setup-external-tools fetches the skeleton +
  compiles tools/schematron/csl-tei-lex0.svrl.xsl; harness runs it over 256 Lex-0
  files, 0 failed asserts. Skeleton URL: github.com/Schematron/schematron
  trunk/schematron/code (need iso_dsdl_include + iso_abstract_expand +
  iso_svrl_for_xslt2 + iso_schematron_skeleton_for_saxon). All three schema
  languages (RNG/Schematron/SHACL) now validated by real external engines.
